axon:
  axonserver:
    servers: ${A6_MGMT_ACCOUNTING_AXON_HOST:localhost:8124}
    enabled: true
    authentication:
      enabled: false
  serializer:
    general: JACKSON # Not with env properties to avoid changing after deploy. Alternatives: events / XSTREAM